<!DOCTYPE html>
<html lang="{{ htmlLanguage }}" class="{{ htmlClass }}">

  {% include "partials/head.nunjucks" %}

  <body class="{{ bodyClass }}">
    {# setting page specific content #}
    {% block content %}
    {% endblock %}

    {# these are added within it's respective nunjucks page #}
    {% block pageSpecificJs %}
    {% endblock %}

    <!-- build:js -->
      <!-- do not remove this build tag -->
    <!-- endbuild -->
  </body>
</html>
